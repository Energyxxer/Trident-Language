package com.energyxxer.trident.util;

/**
 * A class with a boolean method that takes a string. What? What else did you
 * think it was?
 */
public interface StringValidator {
	boolean validate(String str);
}
